scripted_gui = {
	GER_focus_details_scripted_gui= {
		context_type = player_context
		parent_window_name = "zoom_slider_container"
		
		window_name = "GER_focus_details"

        visible = {
			has_focus_tree = GER_focus_tree
        }

        ai_enabled = {
            always = no
        }
    } 
    default_focus_details_gui = {
		context_type = player_context
		parent_window_name = "zoom_slider_container"
		
		window_name = "default_focus_details"

        visible = {
			NOT = {
                has_focus_tree = GER_focus_tree
            }
        }

        ai_enabled = {
            always = no
        }
    } 
}